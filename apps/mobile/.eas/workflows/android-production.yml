name: Android Production Build

on:
  push:
    branches:
      - master
    paths:
      - apps/mobile/**
      - packages/shared/**
      - '!**/*.md'

jobs:
  build_android:
    type: build
    params:
      platform: android
      profile: production
